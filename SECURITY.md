# Security Policy

## Supported versions

Island 尚处于本地开发阶段。公开预览后，只维护最新的 `0.x` 次版本；`1.0` 后再公布长期支持范围。

## Reporting a vulnerability

在仓库公开前，请不要创建公开 Issue 报告可能导致数据丢失、路径越界、任意文件访问、密钥泄漏或远程代码执行的问题。请通过仓库维护者提供的私密联系方式报告，并包含：

- 受影响版本和 Windows 版本
- 最小复现步骤
- 预期与实际结果
- 是否涉及真实用户内容

报告中不要附带真实资料、完整个人路径、API Key 或其他敏感数据。

## Security principles

- 默认不上传文件、正文、路径、搜索记录或使用行为。
- 托管文件删除前验证目标位于 Island `assets` 目录。
- API Key 不进入 SQLite、日志、崩溃报告或导出。
- 日志不记录正文和完整原始路径。
- 数据库迁移前必须备份；导出不得修改源数据。
