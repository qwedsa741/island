{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the Island desktop windows",
  "windows": ["main", "island"],
  "permissions": [
    "core:default",
    "dialog:default",
    "global-shortcut:default"
  ]
}
