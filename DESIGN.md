---
name: Island
direction: 纸墨研究所
description: 温暖、克制、以阅读和知识沉淀为中心的本地工作台
colors:
  light-background: "oklch(0.920 0.015 75)"
  light-surface: "oklch(0.940 0.014 75)"
  light-paper: "oklch(0.985 0.008 78)"
  light-ink: "oklch(0.270 0.018 70)"
  dark-background: "oklch(0.170 0.012 65)"
  dark-surface: "oklch(0.200 0.014 65)"
  dark-paper: "oklch(0.240 0.016 65)"
  dark-ink: "oklch(0.910 0.012 72)"
  primary: "oklch(0.470 0.105 30)"
  success: "oklch(0.480 0.080 150)"
  warning: "oklch(0.630 0.120 75)"
  danger: "oklch(0.530 0.150 28)"
rounded:
  compact: "3px"
  overlay: "6px"
  signature: "14px"
---

# Island 设计系统：纸墨研究所

## 核心印象

Island 是一个私人知识档案馆，而不是 SaaS 管理后台。界面以暖纸、墨色、氧化红和克制的宋体标题建立长期阅读感；资料始终是视觉主角，Agent 只在用户提供上下文后出现。

关键词：

- 温暖但不复古。
- 有文化感但保持工具效率。
- 依靠排版、比例和分隔，不依靠彩色卡片。
- 默认阅读，按需编辑。
- 浅色如纸，深色如夜间书房。

## 色彩

### 浅色纸墨

- 整体背景：暖灰纸色。
- 导航和资料列表：比背景略深的档案纸。
- 内容面：接近象牙白的干净纸张。
- 正文：暖黑墨色。
- 主色：低饱和氧化红，只用于主操作、焦点、链接和当前资料边线。

### 夜间纸墨

- 整体背景：棕黑墨色，不使用蓝黑科技感。
- 内容面：暖深灰。
- 正文：偏暖的浅灰白。
- 主色：提亮珊瑚红。
- 阅读正文保持独立纸张层次，但不使用纯白页面。

主色不得大面积铺满。成功、警告和危险状态必须同时使用文字或图标。

## 字体

UI：

```css
"Segoe UI Variable", "Microsoft YaHei UI", "Segoe UI", sans-serif
```

标题与阅读：

```css
"Noto Serif SC", "Source Han Serif SC", "Songti SC", serif
```

路径和类型缩写：

```css
"Cascadia Code", Consolas, monospace
```

层级：

- 页面标题：20/28px，宋体 Medium。
- 详情标题：20/28px，宋体 Medium。
- 资料标题：14/20px，无衬线 Semibold。
- UI 正文：14/20px，无衬线 Regular。
- 辅助文字：最低 12/16px。
- 阅读正文：默认 17px、1.76 行距、68ch。

避免在标题上普遍使用 700 字重。高级感来自字面、行距和留白，而不是机械粗体。

## 形状与材质

- 导航、按钮、输入框以 3px 紧凑圆角为主。
- 菜单和浮层使用 6px。
- 14px 只保留给悬浮岛等签名组件。
- 文件类型使用单色圆形文字缩写：`PDF`、`WEB`、`TXT`、`MD`、`IMG`、`FILE`。
- 常驻面板无阴影。
- 纸张预览和阅读正文允许一层轻阴影。
- 禁止彩色文件类型方块、玻璃拟态、渐变和卡片墙。

## 主窗口

三栏结构保持不变：

- 216px 导航。
- 资料列表约 40%。
- 详情约 60%。

资料列表是连续档案索引，不是卡片集合。当前资料使用暖色底和右侧 3px 氧化红定位线。类型只使用单色缩写。

详情遵循：

1. 类型、标题、时间和收藏状态。
2. 内容预览。
3. 资料备注。
4. 元数据和组织信息。
5. 固定底部操作。

详情默认展示阅读态；标题和备注只有点击“编辑”后才变成字段。沉浸阅读是唯一主操作，危险操作进入菜单。

## 阅读器

阅读器是 Island 视觉质量最高的区域：

- 阅读画布使用较深的暖纸背景。
- 正文位于独立纸张面。
- 默认 Noto Serif SC 17px / 1.76 / 68ch。
- PDF、网页快照、文本和 DOCX 共享相同工具栏和上下文栏；DOCX 在本地解析为安全的只读正文。
- PDF、图片、DOCX 的加载失败必须显示原因和可恢复操作，禁止以空白画布或破损图片代替错误状态。
- 旧版 DOC、PPT/PPTX、XLS/XLSX、压缩包、可执行文件和未知格式必须显示明确的“系统打开”降级状态，不假装已预览。
- 右栏为目录、标注、关联、Agent。
- 900px 以下彻底移除右栏网格占位，正文使用完整宽度。

## Agent

Agent 使用任务线程和上下文托盘，不使用聊天气泡。未配置模型时应明确说明本地搜索、引用要求、写入审批和隐私状态。

## 组件状态

所有交互组件必须覆盖：

- default
- hover
- active
- focus-visible
- disabled
- loading
- error

焦点使用 2px 主色外环。动效控制在 140–180ms；减少动态效果时取消位移和缩放。

## 禁则

- 不恢复潮汐青或多彩文件类型盒。
- 不把详情永久呈现为编辑表单。
- 不用大面积强调色制造层级。
- 不使用 12px 以下常规文字。
- 不让 Agent 成为默认首页。
- 不给常驻面板添加宽泛阴影。
- 不把空间、设置或处理中页面做成卡片墙。
