# Island UI 视觉方向探索

这是独立于正式产品界面的视觉探索，不修改 `src/` 中的业务实现。

本地查看：

```powershell
npx vite design-explorations --host 127.0.0.1 --port 4178
```

方向：

- `?style=night`：夜航档案馆
- `?style=paper`：纸墨研究所
- `?style=swiss`：瑞士索引系统
- `?style=native`：Windows 原生工坊

截图时追加 `&capture=1` 可隐藏方案切换器。
